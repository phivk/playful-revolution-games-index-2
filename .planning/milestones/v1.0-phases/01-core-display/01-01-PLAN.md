---
phase: 01-core-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.js, src/app/layout.tsx, src/app/page.tsx, src/app/game/[slug]/page.tsx, src/components/GameCard.tsx, src/data/games.json, src/types/game.ts]
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]

must_haves:
  truths:
    - "Game catalog displays ~30 games in scrollable list/grid on mobile"
    - "Each game card shows title, tags, pillars, energy level at a glance"
    - "Tapping a game opens detail view with full facilitation instructions"
    - "Detail view is readable on mobile without horizontal scrolling"
  artifacts:
    - path: "src/data/games.json"
      provides: "Game catalog data"
      min_lines: 200
    - path: "src/app/page.tsx"
      provides: "Catalog view with game grid"
    - path: "src/app/game/[slug]/page.tsx"
      provides: "Game detail view with facilitation instructions"
    - path: "src/components/GameCard.tsx"
      provides: "Game card component"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/data/games.json"
      via: "import and map over games"
    - from: "src/app/game/[slug]/page.tsx"
      to: "src/data/games.json"
      via: "dynamic route with slug lookup"
---

<objective>
Set up Next.js project with game catalog and detail views for mobile-friendly display.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@playful_revolution_web_design_brief.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with static export</name>
  <files>package.json, next.config.js, app/layout.tsx, app/page.tsx</files>
  <action>
    Create Next.js project with the following:
    - Use `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm` to scaffold
    - Configure next.config.js with `output: 'export'` for static site generation
    - Set up basic layout with mobile viewport meta tag
    - Use the paper-like off-white background (#FAFAF7) per design brief
    - Use Inter font from Google Fonts for body text
  </action>
  <verify>
    Run `npm run build` and verify it produces static HTML in `out/` directory
  </verify>
  <done>
    Next.js project builds successfully to static export
  </done>
</task>

<task type="auto">
  <name>Task 2: Create game data with ~30 games</name>
  <files>src/data/games.json, src/types/game.ts</files>
  <action>
    Create TypeScript type for Game with fields:
    - id, slug, title, description
    - tags: string[] (Social Spontaneity, Group Circle Games, Collaborative, Competitive, Ball Games, Theatre Sports, Movement, Table Games)
    - pillars: string[] (Intellectual, Social, Physical)
    - energyLevel: "Low" | "Medium" | "High"
    - materials: string[]
    - setup: string
    - howToPlay: string[]

    Create games.json with 30 diverse games covering all tags/pillars/energy levels.
    Each game needs realistic facilitation instructions (materials, setup, how to play).
  </verify>
  <verify>
    TypeScript compiles without errors, JSON is valid
  </verify>
  <done>
    Game data file exists with 30 games, all required fields populated
  </done>
</task>

<task type="auto">
  <name>Task 3: Build catalog and detail views</name>
  <files>src/app/page.tsx, src/app/game/[slug]/page.tsx, src/components/GameCard.tsx</files>
  <action>
    Create GameCard component:
    - Shows title, tags (as small badges), pillars (icons/text), energy level (visual indicator)
    - Mobile-optimized card size (full width with padding)
    - Tap target covers entire card

    Create catalog page (app/page.tsx):
    - Grid layout: 1 column on mobile, 2 on tablet+
    - Scrollable list of GameCard components
    - Page title "Games"

    Create detail page (app/game/[slug]/page.tsx):
    - Dynamic route for each game via slug
    - Shows: title, description, tags, pillars, energy level
    - Shows facilitation section: materials (list), setup (text), how to play (numbered steps)
    - Back navigation to catalog
    - Mobile-readable: no horizontal scroll, comfortable font sizes (16px base), adequate line height

    Add basic styling:
    - Use bold colors for emphasis (from design brief: #E53935, #43A047, #FDD835, #1E3A8A)
    - Energy levels: Low=blue, Medium=yellow, High=red indicators
    - Tags as colored badges
  </action>
  <verify>
    - Catalog shows 30 games in scrollable grid
    - Each card displays title, tags, pillars, energy at a glance
    - Clicking game navigates to detail page
    - Detail page shows all facilitation instructions
    - No horizontal scroll on mobile viewport (375px width)
  </verify>
  <done>
    Full game catalog and detail views functional on mobile
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - succeeds with static export
2. Catalog page displays all 30 games
3. Game cards show: title, tags (badges), pillars, energy level
4. Detail page accessible via tap, shows all fields
5. Mobile viewport (375px) test: no horizontal scroll, readable text
</verification>

<success_criteria>
- [ ] CORE-01: Game catalog displays ~30 games in scrollable grid
- [ ] CORE-02: Game cards show title, tags, pillars, energy level at a glance
- [ ] CORE-03: Detail view shows full facilitation instructions (materials, setup, how to play)
- [ ] CORE-04: Mobile-friendly layout - readable without horizontal scroll
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-display/01-01-SUMMARY.md`
</output>
