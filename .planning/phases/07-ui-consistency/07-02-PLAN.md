---
phase: 07-ui-consistency
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PlaylistButton.tsx
  - src/components/FilterChips.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "PlaylistButton displays border-3 border-foreground (not border-2) in its inactive state"
    - "PlaylistButton hover shifts colour to revolution-red with no scale transform"
    - "FilterChips Clear all button displays border-3 border-foreground in its default visible state"
    - "FilterChips Clear all button hover shifts colour to revolution-red with no scale transform"
    - "FilterChips Clear all button meets min-h-[44px] touch target"
  artifacts:
    - path: "src/components/PlaylistButton.tsx"
      provides: "Playlist toggle button with correct border and hover pattern"
      contains: "border-3 border-foreground"
    - path: "src/components/FilterChips.tsx"
      provides: "Filter panel with Clear all button using correct border and hover pattern"
      contains: "border-3 border-foreground"
  key_links:
    - from: "PlaylistButton span.className"
      to: "Tailwind border-3 utility"
      via: "border-3 class (custom, replaces border-2)"
      pattern: "border-3 border-foreground"
    - from: "FilterChips Clear all button.className"
      to: "hover:border-revolution-red hover:text-revolution-red"
      via: "Tailwind hover utilities"
      pattern: "hover:border-revolution-red hover:text-revolution-red"
---

<objective>
Update PlaylistButton and the FilterChips "Clear all" button to match the chip visual language: `border-3 border-foreground` inactive state, colour-shift hover only (no scale), and `min-h-[44px]` touch target where applicable.

Purpose: UI-02 requires button elements to share the same border treatment as chips. Currently PlaylistButton uses `border-2` and "Clear all" uses `border-2 border-transparent` — both diverge from the canonical `border-3 border-foreground` pattern.

Output: Two updated files where both button elements use `border-3 border-foreground`, the `hover:border-revolution-red hover:text-revolution-red` pattern, and no scale transforms.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current class strings (before fix) — executor uses these to locate exact changes needed. -->

PlaylistButton.tsx — the span element (line 48):
```tsx
className={`shrink-0 w-8 h-8 flex items-center justify-center rounded-full border-2 border-foreground ${
  inPlaylist
    ? 'bg-playlist-amber text-foreground scale-110'
    : 'bg-background text-foreground hover:text-revolution-red hover:border-revolution-red active:scale-90'
}`}
```

Decisions made for PlaylistButton:
- Change `border-2` to `border-3`
- The button is deliberately small (w-8 h-8 = 32px square) — it is a compact icon-only button nested inside a game card. Do NOT add min-h-[44px] as this would break the card layout. The chip min-height rule applies to standalone filter chips, not this inline icon button.
- Remove `scale-110` from the `inPlaylist` active branch (it's a scale transform on the active/selected state, not hover — remove for consistency)
- Remove `active:scale-90` from the inactive hover branch

After fix:
```tsx
className={`shrink-0 w-8 h-8 flex items-center justify-center rounded-full border-3 border-foreground ${
  inPlaylist
    ? 'bg-playlist-amber text-foreground'
    : 'bg-background text-foreground hover:text-revolution-red hover:border-revolution-red'
}`}
```

FilterChips.tsx — the "Clear all" button (line 129-135):
```tsx
<button
  onClick={onClearAll}
  className={`w-full flex items-center justify-center gap-1.5 px-3 py-2 rounded-lg border-2 border-transparent hover:border-revolution-red text-sm font-bold uppercase tracking-wider text-revolution-red ${hasAnyFilters ? "opacity-100" : "opacity-0 pointer-events-none"}`}
>
```

After fix — change `border-2 border-transparent` to `border-3 border-foreground`, add `min-h-[44px]`, change default text colour from `text-revolution-red` to `text-foreground`, add `hover:text-revolution-red` explicitly:
```tsx
<button
  onClick={onClearAll}
  className={`w-full flex items-center justify-center gap-1.5 px-3 py-2 rounded-lg border-3 border-foreground hover:border-revolution-red hover:text-revolution-red text-sm font-bold uppercase tracking-wider text-foreground min-h-[44px] transition-all duration-100 ${hasAnyFilters ? "opacity-100" : "opacity-0 pointer-events-none"}`}
>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PlaylistButton border and remove scale transforms</name>
  <files>src/components/PlaylistButton.tsx</files>
  <action>
In PlaylistButton.tsx, find the span element's className template literal (currently starts with `shrink-0 w-8 h-8 flex items-center justify-center rounded-full border-2 border-foreground`).

Make the following three changes:
1. Replace `border-2` with `border-3`
2. In the `inPlaylist` branch: remove `scale-110` — change `'bg-playlist-amber text-foreground scale-110'` to `'bg-playlist-amber text-foreground'`
3. In the inactive branch: remove `active:scale-90` — change `'bg-background text-foreground hover:text-revolution-red hover:border-revolution-red active:scale-90'` to `'bg-background text-foreground hover:text-revolution-red hover:border-revolution-red'`

Do NOT add `min-h-[44px]` — this button is a compact icon (w-8 h-8) nested in a card and must stay 32px. Do not change anything else.
  </action>
  <verify>
    <automated>grep -n "border-2\|scale-110\|scale-90" "/Users/phivk/Documents/Philo Personal/projects/playful-revolution-games-index-2/src/components/PlaylistButton.tsx" && echo "FAIL: old classes still present" || echo "PASS: old classes removed"</automated>
  </verify>
  <done>PlaylistButton.tsx uses `border-3 border-foreground`, contains no `scale-110` or `active:scale-90`, and TypeScript build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update FilterChips "Clear all" button border, hover pattern, and sizing</name>
  <files>src/components/FilterChips.tsx</files>
  <action>
In FilterChips.tsx, find the "Clear all" button element (the `<button onClick={onClearAll} ...>` near the bottom of the component).

Replace its className string from:
```
w-full flex items-center justify-center gap-1.5 px-3 py-2 rounded-lg border-2 border-transparent hover:border-revolution-red text-sm font-bold uppercase tracking-wider text-revolution-red ${hasAnyFilters ? "opacity-100" : "opacity-0 pointer-events-none"}
```

To:
```
w-full flex items-center justify-center gap-1.5 px-3 py-2 rounded-lg border-3 border-foreground hover:border-revolution-red hover:text-revolution-red text-sm font-bold uppercase tracking-wider text-foreground min-h-[44px] transition-all duration-100 ${hasAnyFilters ? "opacity-100" : "opacity-0 pointer-events-none"}
```

Key changes:
- `border-2 border-transparent` → `border-3 border-foreground`
- `hover:border-revolution-red` stays, add `hover:text-revolution-red` explicitly
- `text-revolution-red` (always red) → `text-foreground` (neutral by default, red on hover)
- Add `min-h-[44px]` and `transition-all duration-100`

No other changes to FilterChips.tsx.
  </action>
  <verify>
    <automated>grep -n "border-2\|border-transparent\|text-revolution-red" "/Users/phivk/Documents/Philo Personal/projects/playful-revolution-games-index-2/src/components/FilterChips.tsx" && echo "WARN: check if any old classes remain" || echo "PASS: old border/text classes removed from Clear all button"</automated>
  </verify>
  <done>FilterChips.tsx "Clear all" button uses `border-3 border-foreground`, `hover:border-revolution-red hover:text-revolution-red`, `text-foreground`, `min-h-[44px]`. TypeScript build passes.</done>
</task>

</tasks>

<verification>
Run TypeScript type-check:
```
cd "/Users/phivk/Documents/Philo Personal/projects/playful-revolution-games-index-2" && npx tsc --noEmit
```
Confirm zero errors.

Visual spot-check: run `npm run dev`, open the game index. Hover over a game card's playlist button — expect border/icon colour shift, no scale. Open the filter panel, add a filter to make "Clear all" visible, hover it — expect border and text shift to red, no scale.
</verification>

<success_criteria>
- PlaylistButton uses `border-3` (not `border-2`), contains no `scale-110` or `active:scale-90` (UI-02, UI-03)
- FilterChips "Clear all" uses `border-3 border-foreground`, `hover:border-revolution-red hover:text-revolution-red`, `text-foreground`, `min-h-[44px]` (UI-02, UI-03, UI-04)
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-consistency/07-02-SUMMARY.md` following the summary template.
</output>
