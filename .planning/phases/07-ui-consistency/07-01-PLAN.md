---
phase: 07-ui-consistency
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TagChip.tsx
  - src/components/PillarChip.tsx
  - src/components/EnergyChip.tsx
  - src/components/DurationChip.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04

must_haves:
  truths:
    - "TagChip in interactive mode shows border-3 border-foreground when inactive (not selected)"
    - "PillarChip in interactive mode shows border-3 border-foreground when inactive"
    - "EnergyChip in interactive mode shows border-3 border-foreground when inactive"
    - "DurationChip in interactive mode shows border-3 border-foreground when inactive"
    - "Hovering any interactive chip shifts colour to revolution-red — no scale transform occurs"
    - "All interactive chips meet min-h-[44px] touch target"
  artifacts:
    - path: "src/components/TagChip.tsx"
      provides: "Tag chip with correct inactive border and hover pattern"
      contains: "hover:border-revolution-red hover:text-revolution-red"
    - path: "src/components/PillarChip.tsx"
      provides: "Pillar chip with correct inactive border and hover pattern"
      contains: "hover:border-revolution-red hover:text-revolution-red"
    - path: "src/components/EnergyChip.tsx"
      provides: "Energy chip with correct inactive border and hover pattern"
      contains: "hover:border-revolution-red hover:text-revolution-red"
    - path: "src/components/DurationChip.tsx"
      provides: "Duration chip with correct inactive border and hover pattern"
      contains: "hover:border-revolution-red hover:text-revolution-red"
  key_links:
    - from: "interactive chip className string"
      to: "Tailwind border utilities"
      via: "border-3 class (custom, defined in tailwind config)"
      pattern: "border-3 border-foreground"
---

<objective>
Remove scale transforms from all four interactive chip components and confirm the existing border, hover, and sizing classes are correct.

Purpose: Chips are the structural reference for all interactive elements in v1.2 UI Consistency. Scale transforms (`hover:scale-105 active:scale-95`) are inconsistent with the colour-shift-only hover pattern decided for this milestone. Removing them makes chips the clean baseline.

Output: Four updated chip components — TagChip, PillarChip, EnergyChip, DurationChip — with `hover:scale-105 active:scale-95` (and the `transform` prefix) removed from the interactive-mode className string. All other classes remain unchanged.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current interactive className strings (before fix) — executor uses these to locate and remove the scale utilities. -->

TagChip.tsx (line 36):
```
isInteractive ? "border-3 border-foreground" : "border-0"
...
isInteractive ? "transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer" : ""
```
After fix, the second ternary branch becomes:
```
isInteractive ? "min-h-[44px] cursor-pointer" : ""
```

PillarChip.tsx (line 45-48):
```
isInteractive
  ? "border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"
  : ""
```
After fix:
```
isInteractive
  ? "border-3 border-foreground min-h-[44px] cursor-pointer"
  : ""
```

EnergyChip.tsx (line 22-25):
```
isInteractive
  ? "border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"
  : ""
```
After fix:
```
isInteractive
  ? "border-3 border-foreground min-h-[44px] cursor-pointer"
  : ""
```

DurationChip.tsx (line 22-25):
```
isInteractive
  ? "border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"
  : ""
```
After fix:
```
isInteractive
  ? "border-3 border-foreground min-h-[44px] cursor-pointer"
  : ""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove scale transforms from TagChip</name>
  <files>src/components/TagChip.tsx</files>
  <action>
In TagChip.tsx, find the className string for the interactive element. The second ternary branch currently reads:

  `isInteractive ? "transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer" : ""`

Remove `transform hover:scale-105 active:scale-95` so it becomes:

  `isInteractive ? "min-h-[44px] cursor-pointer" : ""`

No other changes. The `border-3 border-foreground` (first ternary branch) and the `hover:border-revolution-red hover:text-revolution-red` in the inactive active-state branch are already correct — leave them untouched.
  </action>
  <verify>
    <automated>grep -n "scale" /Users/phivk/Documents/Philo\ Personal/projects/playful-revolution-games-index-2/src/components/TagChip.tsx && echo "FAIL: scale still present" || echo "PASS: no scale classes"</automated>
  </verify>
  <done>TagChip.tsx contains no `hover:scale-*` or `active:scale-*` or `transform` in the interactive className. The file still builds without TypeScript errors (`npx tsc --noEmit` passes).</done>
</task>

<task type="auto">
  <name>Task 2: Remove scale transforms from PillarChip, EnergyChip, DurationChip</name>
  <files>src/components/PillarChip.tsx, src/components/EnergyChip.tsx, src/components/DurationChip.tsx</files>
  <action>
Apply the same removal to the remaining three chips.

PillarChip.tsx — the interactive ternary branch currently reads:
  `"border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"`
Change to:
  `"border-3 border-foreground min-h-[44px] cursor-pointer"`

EnergyChip.tsx — the interactive ternary branch currently reads:
  `"border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"`
Change to:
  `"border-3 border-foreground min-h-[44px] cursor-pointer"`

DurationChip.tsx — the interactive ternary branch currently reads:
  `"border-3 border-foreground transform hover:scale-105 active:scale-95 min-h-[44px] cursor-pointer"`
Change to:
  `"border-3 border-foreground min-h-[44px] cursor-pointer"`

Do not touch any other classes in any of the three files.
  </action>
  <verify>
    <automated>grep -rn "scale" /Users/phivk/Documents/Philo\ Personal/projects/playful-revolution-games-index-2/src/components/PillarChip.tsx /Users/phivk/Documents/Philo\ Personal/projects/playful-revolution-games-index-2/src/components/EnergyChip.tsx /Users/phivk/Documents/Philo\ Personal/projects/playful-revolution-games-index-2/src/components/DurationChip.tsx && echo "FAIL: scale still present" || echo "PASS: no scale classes"</automated>
  </verify>
  <done>PillarChip.tsx, EnergyChip.tsx, and DurationChip.tsx each contain no `hover:scale-*`, `active:scale-*`, or `transform` in their interactive className strings. TypeScript build passes.</done>
</task>

</tasks>

<verification>
Run TypeScript type-check across the project:
```
cd "/Users/phivk/Documents/Philo Personal/projects/playful-revolution-games-index-2" && npx tsc --noEmit
```
Confirm zero errors.

Visual spot-check: run `npm run dev` and open the filter panel. Hover over any chip — expect colour shift to revolution-red only, no size jump.
</verification>

<success_criteria>
- All four chip files (TagChip, PillarChip, EnergyChip, DurationChip) contain zero scale transform utilities in their interactive className strings
- `border-3 border-foreground` remains present on all interactive chip elements (UI-01)
- `hover:border-revolution-red hover:text-revolution-red` remains present in the inactive state branch of all chips (UI-03)
- `min-h-[44px]` remains present on all interactive chips (UI-04)
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-consistency/07-01-SUMMARY.md` following the summary template.
</output>
